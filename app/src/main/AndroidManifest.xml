<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.geek.profiledash">

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProfileDash">
        <activity
            android:name=".dashUi.ForgotPassword"
            android:theme="@style/NoActionBar" />
        <activity android:name=".dashUi.ProfileActivity" />
        <activity
            android:name=".dashUi.SignInActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".dashUi.SignUpActivity"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".dashUi.SplashActivity"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>